<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.finalproject.dao.BusinessMemberDAO">
	<select id="selectMember" resultType="kr.kh.finalproject.vo.BusinessMemberVO">
		select * from business_member where bm_id = #{bid}
	</select>
	<update id="updateMemberSession">
		update
			business_member
		set
			bm_session_id = #{businessmember.bm_session_id},
			bm_session_limit = #{businessmember.bm_session_limit}
		where
			bm_id = #{businessmember.bm_id}
	</update>
</mapper>