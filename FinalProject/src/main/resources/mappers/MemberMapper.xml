<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.FinalProject.dao.MemberDAO">
<!-- <insert id="insertMember">
	insert into user(user_id, user_aorb, user_pw)
	values(#{user_id}, #{user_aorb}, #{user_pw)
</insert>
<select id="isMember" parameterType="string" resultType="int">
    select count(*) from member where user_id = #{user_id}
</select>
<select id="selectMember" resultType="MemberVO">
	select * from member where me_user_id = #{id}
</select> -->
</mapper>