<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.finalproject.dao.MemberDAO">
	<insert id="insertUser">
		insert into user (user_id, user_aorb, user_phone) 
		values (#{user_id}, #{user_aorb}, #{user_phone})
	</insert>
	<insert id="insertMember">
		insert into member(user_id, me_pw, me_name, me_email)
		values (#{member.user_id}, #{member.me_pw}, #{member.me_name}, #{member.me_email})

	</insert>
	
	<select id="selectMember" resultType="kr.kh.finalproject.vo.MemberVO">
		select * from user where user_id = #{id} 
	</select>
</mapper>